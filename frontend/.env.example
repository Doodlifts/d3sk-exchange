# D3SK Exchange — Frontend Environment Variables
# Copy this to .env and fill in your values

# Network: "testnet" or "mainnet"
VITE_FLOW_NETWORK=mainnet

# Your deployed contract addresses (same address if both deployed to same account)
VITE_D3SK_OFFER_ADDRESS=0x_YOUR_DEPLOYED_ADDRESS
VITE_D3SK_REGISTRY_ADDRESS=0x_YOUR_DEPLOYED_ADDRESS
